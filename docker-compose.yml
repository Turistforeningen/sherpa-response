redis:
  image: redis:2.8
  restart: always

www:
  build: .
  ports:
    - "8080:8080"
  links:
    - redis
  volumes:
    - .:/sherparesponse
  environment:
    - APP_HOST=0.0.0.0
    - APP_PORT=8080
    - FLASKR_SETTINGS=settings.py
  command: python flaskr/flaskr.py
  restart: always
